package automation.platforms.base;

/**
 * Base class for all Model (test data) classes.
 * Models represent the data passed into page actions during test execution.
 */
public abstract class BaseModel {
    // Common fields that most models share can be added here
    // (e.g., createdAt, updatedAt for audit-trail test scenarios)
}
